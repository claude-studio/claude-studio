#!/bin/sh
CMUX=/Applications/cmux.app/Contents/Resources/bin/cmux
[ -x "$CMUX" ] && $CMUX set-status last_commit "$(LANG=ko_KR.UTF-8 git log -1 --format="%cd" --date=format:"%Y-%m-%d %H:%M (%A)")" --icon checkmark --color "#30d158"
